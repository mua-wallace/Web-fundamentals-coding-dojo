// variable declaration

var  myName = "Wallace"; //   string
var  hisName = 'Lapi cedrick'; //string



var  myNumber = '345'; //string


var myBoolean = "true" 

var  myAge = 50; // number

"wallace'  'wallace" // 

"wallace"    //


var isPresent = false //boolean
var isPresent = true  // boolean


// = is used for assignment (assignment operator)
// == is used for comparison  (we compare the values). Returns true if the values are theame and false if not the case
// === is still used for comparison  (we compare the values  and type(data type)). Returns true if both the value and the type are thesame and false otherwise

// Negation of the comparison operator

// negation for == is !=
// negation for === is !==

2 != 8 // true

2 != "2" // false

2 !== "4" // true

2 !== "2" //  false




var myName = "Cedrick" // assigment

a == b // comparison , we comparing the value

2 == 2 // true
2 == -2 // false

// type coersion
type
3 // type is number

"3" // type is string

3 == "3" // true

3 == "4" // false

c === d // comparison , we comparing the value and type(data type)


3 === "3" // false







